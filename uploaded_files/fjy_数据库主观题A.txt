事务（Transaction）是数据库管理系统中执行的一系列操作，这些操作被视为一个单一的工作单元。事务具有四个关键特性：
1. 事务是一个不可分割的工作单位。如果事务的一部分失败，则整个事务将被回滚。
2. 每个事务都应该独立运行，不受其他事务的影响。
3. 一旦事务成功提交，它对数据库所做的更改将是永久性的，即使之后系统出现故障也不会丢失。
4. 事务执行前后，数据库都必须满足所有的预定义规则。